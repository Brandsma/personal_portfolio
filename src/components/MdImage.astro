---
import { Image } from 'astro:assets';

interface Props {
  src: string;
  alt?: string;
}

const { src, alt = '' } = Astro.props;
---

<Image src={src} alt={alt} inferSize loading="lazy" decoding="async" />
